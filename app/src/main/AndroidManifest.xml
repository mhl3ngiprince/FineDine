<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" /> 
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> 
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoj"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoj"
        android:name=".FineDineApplication"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <!-- Splash Activity - restored as launcher -->
        <activity
            android:name=".SplashActivityNew"
            android:exported="true"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Emergency Launcher Activity - kept as backup -->
        <activity
            android:name=".EmergencyLauncherActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <!-- Emergency Activity for crash recovery -->
        <activity
            android:name=".EmergencyActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat" />

        <!-- Logout Activity -->
        <activity
            android:name=".LogoutActivity"
            android:theme="@style/Theme.FineDineAppCompat"
            android:noHistory="true" />

        <!-- All available activities for navigation -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat" />

        <activity
            android:name=".AdminActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat" />

        <activity
            android:name=".ManagerDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".KitchenActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".OrderActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".ReservationActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".InventoryActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".MenuManagementActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".StaffManagementActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation" />

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Settings" />

        <activity
            android:name=".MenuItemDetailActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat" />

        <activity
            android:name=".ReviewsActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Reviews" />

        <activity
            android:name=".EditStaffActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Edit Staff" />

        <activity
            android:name=".BackupActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Backup &amp; Restore" />

        <activity
            android:name=".EmailSettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Email Settings" />

        <activity
            android:name=".CustomerMenuActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineNavigation"
            android:label="Menu" />

        <activity
            android:name=".PaymentActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Payment" />

        <activity
            android:name=".CartActivity"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Your Cart" />

        <activity
            android:name=".CustomerMenu"
            android:exported="true"
            android:theme="@style/Theme.FineDineAppCompat"
            android:label="Menu" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Notification Receiver -->
        <receiver
            android:name=".NotificationReceiver"
            android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.finedine.rms.NOTIFICATION_ACTION" />
            </intent-filter>
        </receiver>

        <!-- File Provider for image handling -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Meta-data for Firebase -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/primary_green" />
    </application>
</manifest>